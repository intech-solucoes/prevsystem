using Intech.PrevSystem.Entidades;
using System;

namespace System
{
    //public static class TaxaEvolPerfilExtensao
    //{
    //    public static decimal CalcularTaxaMinimaMensal(this TaxaEvolPerfilEntidade taxa) =>
    //        CalculaTaxaMensal(taxa.RENTABILIDADE_MINIMA.Value);

    //    public static decimal CalcularTaxaMediaMensal(this TaxaEvolPerfilEntidade taxa) =>
    //        CalculaTaxaMensal(taxa.RENTABILIDADE_MEDIA.Value);

    //    public static decimal CalcularTaxaMaximaMensal(this TaxaEvolPerfilEntidade taxa) =>
    //        CalculaTaxaMensal(taxa.RENTABILIDADE_MAXIMA.Value);

    //    private static decimal CalculaTaxaMensal(decimal valorAnual)
    //    {
    //        decimal mensal = 1M / 12M;
    //        decimal valor = valorAnual / 100 + 1;
    //        return (decimal)(Math.Pow((double)valor, (double)mensal) - 1) * 100;
    //    }
    //}
}